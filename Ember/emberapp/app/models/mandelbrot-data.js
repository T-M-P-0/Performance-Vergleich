export class MandelbrotData{
    constructor(width, height, reStart, reEnd, imStart, imEnd){
        this.width = width;
        this.height = height;
        this.reStart = reStart;
        this.reEnd = reEnd;
        this.imStart = imStart;
        this.imEnd = imEnd;
    }
}